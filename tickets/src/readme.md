Mocking (faking) Imports with Jest

i. Find the file that we want to 'fake'
ii. In the same dir, create a folder called '**mocks**'
iii. In that folder, create a file with an identical name to the file we want to fake
iv. Write a fake implementation
v. Tell jest to use that fake file inout test file.
